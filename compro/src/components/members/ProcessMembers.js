import React from 'react'

const ProcessMembers = ({processImg}) => {
    return (
        <div style={{margin: '5rem 0 0 0'}}>
            <img src={processImg} alt="proccess traveli" width="100%"/>
        </div>
    )
}

export default ProcessMembers
